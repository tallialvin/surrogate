had
contact_planning : the same file in my locals
create_data_set : to create the data set
stl2ply : mesh to point cloud
add_function : function that i added to create the point cloud of target and obstacle

model_4-30 is the current result model
m2v(vertice based) = 88%
m2n(edge based) = 90%
